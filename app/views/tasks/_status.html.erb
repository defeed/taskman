<% if @task.completed %>
	<%= link_to image_tag('checkbox-checked.png'), toggle_list_task_url(@list, @task), :method => :post, :remote => true %>
<% else %>
	<%= link_to image_tag('checkbox.png'), toggle_list_task_url(@list, @task), :method => :post, :remote => true %>
<% end %>